library(tidyverse)
summs <- d %>%
group_by(fert, irr) %>%
summarise(mean = mean(y), se = sd(y)/sqrt(n()), .groups = "drop")
library(ggplot2)
ggplot(summs, aes(irr, mean, group=fert)) +
geom_line() +
geom_point(size = 2) +
geom_errorbar(aes(ymin=mean-se, ymax=mean+se), width=.2)+
facet_wrap(~fert, nrow=1) +
labs(x="Irrigation", y="Mean Yield",
title="Interaction of Fertilizer vs Irrigation") +
theme_classic(base_size = 12)
library(tidyverse)
# Importing the dataset
d <- read.csv("cropdata.csv")
setwd("D:/RDSML April 2025/R-for-Data-Science-and-Machine-Learning-with-NBICT-LAB-Batch-8/RDSML-Day-40 👉 Two-way ANOVA in R (Part-2)")
library(tidyverse)
# Importing the dataset
d <- read.csv("cropdata.csv")
summs <- d %>%
group_by(fert, irr) %>%
summarise(mean = mean(y), se = sd(y)/sqrt(n()), .groups = "drop")
library(ggplot2)
ggplot(summs, aes(irr, mean, group=fert)) +
geom_line() +
geom_point(size = 2) +
geom_errorbar(aes(ymin=mean-se, ymax=mean+se), width=.2)+
facet_wrap(~fert, nrow=1) +
labs(x="Irrigation", y="Mean Yield",
title="Interaction of Fertilizer vs Irrigation") +
theme_classic(base_size = 12)
# Fit the model
m <- aov(y~fert+irr+fert*irr)
# Fit the model
m <- aov(y ~ fert+irr+fert*irr, data=d)
View(d)
# Importing the dataset
d <- read.csv("cropdata.csv")
summary(m)
#Assumptions visual checks
plot(m)
#Assumptions visual checks
plot(m)
#Assumptions visual checks
par(mfrow = c(2,2))
#Assumptions visual checks
par(mfrow = c(2,2))
#Assumptions visual checks
par(mfrow = c(2,2))
plot(m
library(tidyverse)
par(mfrow=c(2,2))
plot(m)
par(mfrow=c(1,1))
#Assumptions visual checks
par(mfrow=c(2,2))
par(mfrow=c(1,1))
par(mfrow=c(1,1))
# Statistical test to check normal distribution
# Shapiro-WIlk on residuals (normality)
residuals(m)
# Statistical test to check normal distribution
# Shapiro-WIlk on residuals (normality)
res <- residuals(m)
shapiro.test(res)
# Levene's test for equal variances
library(car)
library(car)
leveneTest(y ~ fert*irr, data=d, center=median())
library(car)
leveneTest(y ~ fert*irr, data=d, center=median)
# Interaction contrasts (simple effects):
# effect of fertlizers at each irrigation level
library(emmeans)
contrast(emmeans(m, ~ fert|irr, method="pairwise", adjust="tukey"))
# Effect of irrigation for each fertilizer (N, N+P)
library(emmeans)
contrast(emmeans(m, ~ irr|fert, method="pairwise", adjust="tukey"))
